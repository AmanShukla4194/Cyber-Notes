import { createContext } from "react";

const NoteContext = createContext(); // syntax to creating a new context 
// we will put all the states related to our notes here in this context here

export default NoteContext;